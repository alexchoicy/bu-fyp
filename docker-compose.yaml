services:
  # backend:
  #   build:
  #     context: .
  #     dockerfile: apps/backend/Dockerfile
  #   env_file:
  #     - ./docker/.env.backend
  #   environment:
  #     - PORT=3001
  #   volumes:
  #     - ./docker/appConfig.yaml:/app/apps/config/appConfig.yaml
  #     - ./docker/data/library:/app/data/library/data
  #     - ./docker/cache:/app/data/library/cache
  #   ports:
  #     - "5001:3001"
  #   depends_on:
  #     - db
  #   networks:
  #     - music
  #
  # web:
  #   build:
  #     context: .
  #     dockerfile: apps/web/Dockerfile
  #   env_file:
  #     - ./docker/.env.web
  #   ports:
  #     - "5000:3000"
  #   depends_on:
  #     - backend
  #   networks:
  #     - music

  db:
    image: postgres
    environment:
      - POSTGRES_DB=fypDev
      - POSTGRES_USER=fyp
      - POSTGRES_PASSWORD=fyp
    volumes:
      - pgdata:/var/lib/postgresql
    ports:
      - "5432:5432"
    networks:
      - fyp

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin4_container
    restart: always
    ports:
      - "8888:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: user-name@domain-name.com
      PGADMIN_DEFAULT_PASSWORD: strong-password
    volumes:
      - pgadmin-data:/var/lib/pgadmin
    networks:
      - fyp

volumes:
  pgdata:
  pgadmin-data:

networks:
  fyp:
    driver: bridge
